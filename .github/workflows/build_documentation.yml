name: Build documentation

on:
  push:
    branches:
      - main
      - doc-builder*
      - v*-release

jobs:
   build:
    uses: huggingface/doc-builder/.github/workflows/build_main_documentation.yml@use_hf_hub_final
    with:
      commit_sha: ${{ github.sha }}
      package: accelerate
    secrets:
      token: ${{ secrets.HUGGINGFACE_PUSH }}
