name: Delete dev documentation

on:
  pull_request:
    types: [ closed ]


jobs:
  delete:
    uses: huggingface/doc-builder/.github/workflows/delete_doc_comment.yml@update_comment_bot
    with:
      pr_number: ${{ github.event.number }}
      package: accelerate
    secrets:
      token: ${{ secrets.HF_DOC_PUSH }}
      comment_bot_token: ${{ secrets.HUGGINGFACE_PUSH }}
